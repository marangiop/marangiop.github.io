source "https://rubygems.org"

# This is the exact gem GitHub Pages uses to build your site.
# Using it locally means what you see on your machine matches what gets published.
gem "github-pages", group: :jekyll_plugins
